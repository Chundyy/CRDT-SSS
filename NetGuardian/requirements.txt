# NetGuardian - Dependências Python
# Instalar com: pip install -r requirements.txt

# Segurança e Autenticação
bcrypt==4.1.2
cryptography==41.0.7

# Base de Dados
psycopg2-binary==2.9.9  # PostgreSQL (opcional)

# Configuração
python-dotenv==1.0.0

# Interface Gráfica
Pillow==10.1.0
customtkinter==6.1.0

# SFTP / SSH
paramiko==3.4.0

# Utilitários
python-dateutil==2.8.2
requests==2.31.0
psutil==5.9.5
typing-extensions==4.8.0

# Segurança / Hashing helpers
passlib==1.7.4

# Ferramentas de build / empacotamento
pyinstaller==5.13.0
setuptools>=65.0.0
wheel>=0.38.0

# Opcional / utilitários adicionais que podem ser úteis
python-magic==0.4.27  # file type detection (libmagic)
python-benedict==0.25.3  # dict utilities (optional)

# OBS:
# - Ajuste versões se necessário. Algumas dependências (tkinter) são fornecidas pelo SO.
# - Se preferir não fixar versões, remova os '==<version>' conforme necessário.
